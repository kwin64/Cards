type RegistrationStateType = {}
export type ActionsType = ''

const initialStateRegistration: RegistrationStateType = {}

export const registrationReducer = (state: RegistrationStateType = initialStateRegistration, action: ActionsType): RegistrationStateType => {

    return state
}